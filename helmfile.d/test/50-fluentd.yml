repositories:
- name: bitnami
  url: https://charts.bitnami.com/bitnami

releases:
- name: bitnami
  namespace: logging
  createNamespace: true
  chart: bitnami/fluentd
  version: 5.1.12
  wait: true
  values: 
    - ./values/fluentd-values.yml